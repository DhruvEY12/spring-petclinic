pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh './mvnw clean package -Dcheckstyle.skip'
      }
    }
    stage('Scan') {
      steps {
        sh 'grype dir:/var/lib/jenkins/workspace/Grype_Scan --scope AllLayers'
      }
    }
  }
}
