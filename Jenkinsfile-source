pipeline {
  agent any
  stages {
    stage('Scan') {
      steps {
        sh 'grype dir:/var/lib/jenkins/workspace/Grype_Scan -o json --file grype_result1.json --scope AllLayers'
      }
    }
  }
}
